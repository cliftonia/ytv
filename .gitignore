.superpowers/
local.properties
.gradle/
build/
*.iml
.idea/
.DS_Store

# Signing material. Kept in ~/.ytv and mirrored into GitHub secrets - never here, because this
# repository is public and the key is the only thing that lets a build update an installed app.
*.jks
*.keystore
keystore-*.txt
__pycache__/
*.pyc

# Working notes for whoever picks this up next. Deliberately untracked: it is a snapshot of
# open problems and dead ends, which would be stale the moment it was committed.
HANDOVER.md
